<h1 style="text-align:center" class="velotitle">Go Velo - Edit Details</h1>
<br class="clear"/>


	<div class="booyah-box col-xs-10 col-xs-offset-1">
	<%= simple_form_for @place do |f| %>
		<%= f.input :name %>
		<%= f.input :address %>
		<%= f.input :description %>
		<br />
		<%= f.submit "Update", :class => "btn" %>
	<% end %>
	
	</div>

<br class="clear"/>
